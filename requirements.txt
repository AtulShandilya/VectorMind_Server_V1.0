fastapi>=0.104.0
uvicorn[standard]>=0.24.0
openai>=1.3.0
pypdf>=3.17.0
python-dotenv>=1.0.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.22.5
# Note: For 2GB RAM servers, consider using CPU-only PyTorch:
# torch --index-url https://download.pytorch.org/whl/cpu
# Or install torch separately: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For GPU support, use: pip install faiss-gpu instead of faiss-cpu

